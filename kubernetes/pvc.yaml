apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: data-pvc
  namespace: apps
spec:
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce 