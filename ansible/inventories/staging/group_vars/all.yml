# inventories/staging/group_vars/all.yml
new_user_name: deployer
new_user_password: "{{ vault_user_password }}"   # pakai ansible-vault
new_user_groups:
  - sudo
  - docker

ssh_public_key_file: /Users/eep/.ssh/id_ed25519.pub

# Hardened SSH
ssh_port: 22
ssh_permit_root_login: "no"
ssh_password_authentication: "no"