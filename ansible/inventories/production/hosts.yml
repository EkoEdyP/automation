all:
  children:
    app:
      hosts:
        app-vm:
          ansible_host: 192.168.1.10
          ansible_user: ubuntu

    gateway:
      hosts:
        gateway-vm:
          ansible_host: 192.168.1.11
          ansible_user: ubuntu